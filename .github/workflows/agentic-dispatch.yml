name: Cross-Repo Test
on:
  issues:
    types: [labeled]
jobs:
  call-test:
    uses: NewOpenBSS/agentic/.github/workflows/test-reusable.yml@main
    with:
      message: "triggered by label=${{ github.event.label.name }}"
