name: Structured Test
on:
  issues:
    types: [labeled]
jobs:
  call-test:
    if: github.event.label.name == 'in-design'
    uses: NewOpenBSS/agentic/.github/workflows/test-reusable.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repository: ${{ github.repository }}
    secrets: inherit
